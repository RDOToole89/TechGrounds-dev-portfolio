const today = new Date();
today.getMonth();

// TypeScript know the methods on the Date class
// It will throw an error if you try to acces something that doesnt exist
// today.kfjkajfs

const person = {
  age: 20,
};

class Color {}
const red = new Color();
